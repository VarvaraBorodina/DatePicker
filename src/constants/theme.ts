const COLORS = {
  MAIN_COLOR: '#FFFFFF',
  PRIMARY_COLOR: '#000000',
  ERROR_COLOR: '#FC0377',
}

const SPACES = {
  S: 8,
  M: 15,
}

const OPACITIES = {
  XS: '00',
  S: '20',
  M: '30',
  L: '60',
  XL: '',
}

const SIZES = {
  INPUT_WIDTH: 250,
}

const FONTS = {
  MAIN: 'Open Sans',
}

const FONT_SIZES = {
  M: 15,
}

const FONT_WEIGHT = {
  S: 400,
}

export default {
  COLORS,
  FONT_SIZES,
  SPACES,
  SIZES,
  FONTS,
  OPACITIES,
  FONT_WEIGHT,
}
